Spec-Driven Task Prompt — SPEC C1: Consolidation & De-scaffolding
0. Prime directive
Streamline the microplastics ERA (probabilistic_risk_characterization.html; edit the .Rmd/.qmd source) by removing/relegating development scaffolding and reorganizing flow. This is a structure-and-presentation pass only. No live number may change. Every headline RQ, P(RQ>1), CF, EED, HC5, slope, and sensitivity value in the final render must be byte-identical (within floating-point tolerance) to the current production render. If any edit would change a number, stop and flag it rather than proceeding.
1. Hard guardrail (implement this first)
1.	Snapshot before touching anything. Add a first-pass chunk that writes a machine-readable snapshot (data_output/c1_number_snapshot.json) of: river & ocean Food Dilution and Tissue Translocation RQ_p50 and P(RQ>1) at HC5 and HC10 (1D and MC2D); the three CF medians + 90% CIs; river/ocean EED q50/q95; the canonical 9-row C-PSD slope table (a_cpsd/a_psd/LOD/n_bins); the SPEC Q1 max|Δ|; and the slope-structural-SD sensitivity grid. After all C1 edits and a clean re-render, add a final chunk that recomputes the same values and stopifnot() they equal the snapshot within 1e-8 (exact for integers/LODs).
2.	Seed-locality precondition. Relocating or deleting chunks must not perturb any RNG draw. Verify every stochastic chunk sets its own local set.seed(...) (many already do — mc2d_risk(seed = 1), the sensitivity grid's set.seed(1000 + ...), cf_case_summary()'s set.seed(4000 + nchar(...))). For any global-seed-dependent object (e.g., the top-level alpha <- alpha_dist(...)), pin a local seed immediately before it so order-independence holds. Do this before moving anything.
3.	Relocate, don't recompute. Moving a block to an appendix must carry its exact code unchanged; do not re-fit, re-bootstrap, or re-derive.
4.	Single source of truth. The new front-matter results box and any moved summaries must reference live objects via inline R (`r ...`), never retyped literals.
5.	Work on branch c1-consolidation; commit per subsection; log every move/delete in docs/CHANGELOG_hoffman_revisions.md.
2. Keep / Relegate / Delete manifest
| Item (current location) | Action | Note ||---|---|---|| Overview / intro (§1) | Keep + revise | Add front-matter results box (C1.1); remove "see §15.3 for the real numbers" deferral. || C-PSD fitting (§4.x) | Keep | Reduce to one canonical slope table (C1.8). || Step 2 rescaling CF, corrected/current only (§5.1) | Keep | || Step 3 EED, hazard pSSD++, RQ 1D + MC2D for river & ocean | Keep | These are the live assessment. || Koelmans CF = 8.32 guard (§5.1.0) | Keep as silent test | Convert rendered prose to a quiet stopifnot/testthat; keep one-line "convention check passed." || SPEC Q1 bin-identity result | Keep one line | max|Δa_cpsd| = 8.44e-15; n_bins match — everything else → appendix. || Slope-structural-SD sensitivity, piecewise-CF bound, depth-stratified sensitivity | Keep, consolidated | Merge into one "Sensitivity & structural uncertainty" section (C1.7). || Sediment provenance finding + exclusion decision + Redondo hazard benchmark | Keep, condensed | One short subsection (C1.6). || Literature-anchored benchmark (if present) | Keep | || SPEC 1 all-matrix CF before/after (legacy cumulative) (§13) | Relegate → Appendix A | Convention-validation history. || SPEC 1b Food Dilution RQ decomposition (legacy legacy_cumulative_av_params() re-runs) (§13.1) | Relegate → Appendix A | Full extra pSSD++ runs; keep out of body. || SPEC 3b/3c one-at-a-time RQ decomposition (§14.1) | Relegate → Appendix A | || Segur Python-vs-R full comparison (§12.1.2) | Relegate → Appendix B + suppress dump | C1.3. || Sediment illustrative EED/RQ pipeline + SPEC 1e PSD-shift grid (§11.4/§11.5.2) | Relegate → Appendix C | Non-quantitative; keep exclusion + benchmark in body. || SPEC 1e-iii time-varying bioaccessibility TODO block (§11.5.3) | Relegate → Appendix C | Reduce body to a 2-line "future work." || Water–sediment coherence table (§SPEC 6) | Relegate → Appendix C (trimmed) | Interpretive; not a headline. || Morris elementary effects (§10.1, "beta test") | Delete from body | Currently emits NA placeholders; see C1.5. || Raw Python __main__ namespace dump (the runpy.run_path chunk output) | Delete from render | include=FALSE; leaks local paths + memory addresses (C1.3). || fiber_cf / plastic_pf "illustrative; not applied" code | Delete | Not used in any live result. || Duplicate slope tables (MPsizeBase/alpha table, Segur table, SPEC 6 table, per-matrix cat() prints) | Delete duplicates | Keep one canonical (C1.8). || Repeated "development-scale / re-check at full budget" disclaimers | Delete or downgrade | C1.10 (decision-gated). || Repeated "sediment non-quantitative" caveats (~7×) | Reduce to one | C1.6/C1.11. || Repeated "~10⁷ is the legacy error, not the result" caveats | Delete from body | Redundant once legacy → Appendix A. || TODO(cite) polymer-density placeholder | Resolve or remove pathway | C1.9. |
3. Specific actions
C1.1 — Front-matter results box
Immediately after the Overview, add a concise results box (populated by inline R refs to live objects) stating: river and ocean Food Dilution and Tissue Translocation HC5 RQ_p50 and P(RQ>1); the exposure single-slope↔piecewise bound; that Food Dilution is risk-dominant and Tissue Translocation is below threshold at the median; and that sediment is excluded (with a one-line reason). Remove the current deferral note "No number in this introductory paragraph should be read as the current corrected result; see Section 15.3 for that." 1 — the box makes §15.3 the confirmation, not the first appearance.
C1.2 — Restructure into a methods spine + appendices
Reorder the body to a single linear spine: Results box → PSD/C-PSD → CF rescaling → EED → Hazard (pSSD++) → RQ (river, ocean) → Sensitivity & structural uncertainty → Sediment (excluded) → Conclusions. Move all convention-history, validations, and illustrative machinery to appendices (§5). Drop the SPEC N tags from section titles (they read as an internal changelog); record SPEC provenance in the changelog file instead.
C1.3 — Suppress the Python dump; collapse the Segur validation
Set the Python runpy.run_path(...) chunk to include=FALSE (or results='hide', echo=FALSE) so the raw __main__ namespace dump — module memory addresses, local paths like C:\Users\Scott.Coffin\..., and the full records list printed twice 1 no longer renders. In the body, keep only one sentence: the R fit and Segur PSD_fit.py are bit-identical after the SPEC Q1 populated-bin reconciliation (max|Δa_cpsd| = 8.44e-15, matching n_bins for all 9 PSDs). 1 Move the comparison table itself to Appendix B.
C1.4 — Relegate legacy-reversion decompositions
Move to Appendix A, unchanged in computation: the SPEC 1 all-matrix CF before/after table (legacy cumulative vs corrected differential), 1 the SPEC 1b decomposition that re-runs the pSSD++ hazard alignment and 1D risk for each matrix with legacy_cumulative_av_params(), 1 and the SPEC 3b/3c one-at-a-time reversion table. 1 In the body, replace with one sentence: "The differential-slope convention (length, area, volume, mass) is the current basis; Appendix A quantifies the effect of each prior convention error." Delete the standalone "No Food Dilution RQ figure should be read as ~10⁷ …" 1 caveats from the body (they belong with the Appendix A legacy material).
C1.5 — Morris: remove from the body
The Morris section is non-functional in this render — "The Morris elementary effects could not be computed for this run (all model outputs returned NA … rerun with a larger budget before interpreting)," 1 and it is off by default (run_morris_sensitivity = FALSE) because it repeatedly rebuilds small PSSD++ hazard models and can make renders slow or fragile. 1 Because C1 may not change numbers or re-run compute, do not attempt to fix Morris here. Remove the empty section (and its NA placeholder figure) from the body; leave a one-line "future work" note that a functioning global sensitivity analysis (fixed Morris budget) is deferred to a dedicated compute pass. (Flag to the user: Morris did not run in the "production" render — fixing it is a separate compute task, out of C1 scope.)
C1.6 — Consolidate the sediment pathway
In the body, reduce sediment to one short subsection: (a) the provenance finding + exclusion decision (extrap_conc_PPL is a per-liter/processing-volume column with no dry-mass basis, carried through as a placeholder; flagged non-quantitative and excluded from the headline 1), and (b) the hazard-side Redondo-Hasselerharm benchmark. Move the illustrative EED/RQ pipeline and the SPEC 1e PSD-shift grid to Appendix C. State the non-quantitative caveat once (it currently appears ~7×, e.g., spans 401–405). Keep the exclusion note that the nominal sediment Food Dilution RQ (≈135,540, P = 100%) fails the benthic-plausibility guard by ~11 orders of magnitude and is excluded. 1
C1.7 — Consolidate sensitivity analyses
Merge into one "Sensitivity & structural uncertainty" section: the slope-structural-SD grid (structural_sd = 0, 0.25, 0.5, 1.0), 1 the piecewise-CF bound, and the depth-stratified sensitivity. Present as a single narrative with one summary table; keep each underlying computation unchanged.
C1.8 — De-duplicate the slope table
Keep one canonical 9-row (matrix × shape) C-PSD slope table with a_cpsd, a_psd, LOD, n_bins, R². Delete the duplicate renderings in the MPsizeBase/"this study" table, the Segur comparison (→ Appendix B), the SPEC 6 coherence table (→ Appendix C, or reference the canonical), and the per-matrix cat() console prints. Reference the canonical table by cross-link everywhere else.
C1.9 — Dead code and placeholders
Delete the fiber_cf / plastic_pf "illustrative; not applied" block. 1 Resolve the TODO(cite) polymer-density table 1 — either cite a canonical density source or drop the density-resolved pathway from the body (it feeds diagnostics only). Reduce the SPEC 1e-iii time-varying-bioaccessibility TODO block 1 to a two-line future-work note (full text → Appendix C).
C1.10 — Resolve the development-scale language (decision-gated)
The render repeatedly hedges its own numbers — e.g., "All figures in this section are development-scale (1000 replicates) and are flagged for … a full-budget production re-run before external release." 1 Add a single is_production_budget flag in the setup chunk. If TRUE (the stated case — 1000 replicates is the production budget), remove every "development-scale / re-check at full budget / directionally informative rather than final" disclaimer and state the budget once in Methods. Retain genuine scientific caveats (structural slope uncertainty, sediment exclusion, sparse ocean-fiber fit, limited tox dataset). (Confirm with the user that 1000 replicates is the intended production budget before deleting the hedges.)
C1.11 — Caveat hygiene
Each distinct caveat appears once, at its canonical location: sediment-non-quantitative (once, C1.6); sparse ocean-fiber (once, at the slope table); structural slope uncertainty (once, in Sensitivity); legacy-convention history (once, Appendix A intro). Remove all duplicated instances.
4. Appendix structure (create these)
•	Appendix A — Convention-validation history: SPEC 1 CF before/after, SPEC 1b RQ decomposition, SPEC 3b/3c one-at-a-time reversions, and the "~10⁷ legacy" explanation.
•	Appendix B — C-PSD implementation validation: Segur PSD_fit.py vs native R comparison table + SPEC Q1 reconciliation detail (Python dump suppressed).
•	Appendix C — Sediment (non-quantitative) and exploratory diagnostics: illustrative sediment EED/RQ, SPEC 1e PSD-shift grid, SPEC 1e-iii time-varying-bioaccessibility method, water–sediment coherence.
5. Acceptance criteria & deliverables
•	[ ] Branch c1-consolidation; commit per subsection; changelog updated with every move/delete.
•	[ ] Pre/post number snapshot chunk present; final stopifnot confirms all live numbers unchanged (1e-8 tolerance; exact for LODs/integers). This is the release gate.
•	[ ] Front-matter results box (inline-R-referenced) present; §-body follows the linear spine; SPEC labels removed from headings.
•	[ ] Python namespace dump no longer in the render; Segur comparison + legacy decompositions + illustrative sediment in Appendices A–C.
•	[ ] Morris empty section removed from body (with the flagged note to the user).
•	[ ] One canonical slope table; each caveat once; dead code and resolved/removed placeholders.
•	[ ] is_production_budget handled per user confirmation.
6. Guardrails / do-not
•	Do not change, re-fit, re-bootstrap, or re-seed any live computation — relocation only. The pre/post snapshot must match.
•	Do not fix or re-run Morris in this pass (compute change; out of scope) — remove it and flag.
•	Do not delete genuine scientific caveats (structural uncertainty, sediment exclusion, sparse-fit, limited dataset) — only de-duplicate and de-scaffold.
•	Do not alter the differential-slope conventions, the sediment exclusion decision, the piecewise bound, or any guard.
•	If any structural edit would move a number, stop and report rather than proceed.
References
1.	probabilistic_risk_characterization.html. Internal reference: file:11528#chars=4463-4579. Accessed 2026-07-29.
